import 'server-only';

import { and, asc, desc, eq } from 'drizzle-orm';
import { db } from '@/db';
import { dayReviews, focusSessions, glowInboxItems, lifeModes, maintenanceForecasts, personalRules } from '@/db/schema/adaptive-os';
import { buildPersonalContext, type PersonalContext } from '@/lib/intelligence/context';

export type LifeModeSlug = 'normal' | 'low-energy' | 'sick' | 'work-day' | 'deep-work' | 'social' | 'travel' | 'reset' | 'beauty-maintenance' | 'catch-up';
export type NowAction = { id:string; title:string; reason:string; href:string; source:'task'|'reminder'|'habit'|'routine'|'event'; score:number; estimatedMinutes:number; energyCost:'low'|'medium'|'high'; canDoNow:boolean };
export type AdaptiveSnapshot = { context:PersonalContext; activeMode:{id:string;name:string;slug:string;maxMajorTasks:number;energyTarget:number|null}|null; rules:{id:string;title:string;ruleType:string;priority:number}[]; now:{availableMinutes:number|null;primary:NowAction|null;alternatives:NowAction[];protected:string[];hiddenCount:number}; inboxCount:number; maintenance:{id:string;domain:string;title:string;dueAt:Date|null;urgency:string;recommendation:string|null}[]; systemHealth:{domain:string;status:'stable'|'attention'|'behind';reason:string}[] };

const DEFAULT_MODES:Array<{name:string;slug:LifeModeSlug;description:string;energyTarget:number;maxMajorTasks:number;workoutPolicy:string;routinePolicy:string;schedulingPolicy:string}>=[
{name:'Normal Day',slug:'normal',description:'Balanced default day.',energyTarget:6,maxMajorTasks:3,workoutPolicy:'normal',routinePolicy:'full',schedulingPolicy:'balanced'},
{name:'Low Energy',slug:'low-energy',description:'Protect essentials and reduce cognitive load.',energyTarget:3,maxMajorTasks:1,workoutPolicy:'recovery',routinePolicy:'essentials',schedulingPolicy:'light'},
{name:'Sick Mode',slug:'sick',description:'Recovery first. Keep only health, medication, hydration, and critical commitments.',energyTarget:1,maxMajorTasks:0,workoutPolicy:'rest',routinePolicy:'minimum',schedulingPolicy:'critical-only'},
{name:'Work Day',slug:'work-day',description:'Protect work, commute, meals, and a small number of personal priorities.',energyTarget:5,maxMajorTasks:2,workoutPolicy:'short',routinePolicy:'compact',schedulingPolicy:'work-buffered'},
{name:'Deep Work',slug:'deep-work',description:'Fewer interruptions and longer focus blocks.',energyTarget:7,maxMajorTasks:2,workoutPolicy:'normal',routinePolicy:'compact',schedulingPolicy:'focus-blocks'},
{name:'Social Day',slug:'social',description:'Protect preparation, travel, and recovery around plans.',energyTarget:5,maxMajorTasks:1,workoutPolicy:'optional',routinePolicy:'beauty-forward',schedulingPolicy:'event-buffered'},
{name:'Travel Mode',slug:'travel',description:'Prioritize itinerary, packing, documents, timing, and essentials.',energyTarget:5,maxMajorTasks:2,workoutPolicy:'flexible',routinePolicy:'travel',schedulingPolicy:'travel'},
{name:'Reset Day',slug:'reset',description:'Home, planning, maintenance, and preparation.',energyTarget:5,maxMajorTasks:3,workoutPolicy:'light',routinePolicy:'reset',schedulingPolicy:'reset'},
{name:'Beauty Maintenance',slug:'beauty-maintenance',description:'Prioritize hair, skincare, grooming, and appointments.',energyTarget:5,maxMajorTasks:2,workoutPolicy:'light',routinePolicy:'beauty-full',schedulingPolicy:'maintenance'},
{name:'Catch-Up',slug:'catch-up',description:'Triage overdue work and unblock projects without overloading the day.',energyTarget:6,maxMajorTasks:3,workoutPolicy:'short',routinePolicy:'essentials',schedulingPolicy:'triage'}];
const DEFAULT_RULES=[
{title:'Protect medication visibility',ruleType:'health',priority:100,condition:{itemType:'medication',completed:false},effect:{alwaysVisible:true}},
{title:'Work days stay realistic',ruleType:'scheduling',priority:90,condition:{mode:'work-day'},effect:{maxMajorTasks:2}},
{title:'Low energy protects essentials',ruleType:'scheduling',priority:95,condition:{mode:'low-energy'},effect:{hideOptional:true,maxMajorTasks:1}},
{title:'Sick mode removes noncritical pressure',ruleType:'scheduling',priority:100,condition:{mode:'sick'},effect:{criticalOnly:true}},
{title:'Do not start long work before an event',ruleType:'time',priority:80,condition:{minutesBeforeEventLessThan:45},effect:{maxTaskMinutes:25}},
{title:'Hair wash needs a real time block',ruleType:'beauty',priority:75,condition:{keyword:'hair wash'},effect:{minimumMinutes:120}}];

export async function ensureAdaptiveDefaults(userId:string){const[modes,rules]=await Promise.all([db.select({id:lifeModes.id}).from(lifeModes).where(eq(lifeModes.userId,userId)).limit(1),db.select({id:personalRules.id}).from(personalRules).where(eq(personalRules.userId,userId)).limit(1)]);if(!modes.length)await db.insert(lifeModes).values(DEFAULT_MODES.map((mode,index)=>({...mode,userId,isActive:index===0})));if(!rules.length)await db.insert(personalRules).values(DEFAULT_RULES.map(rule=>({...rule,userId,enabled:true,source:'system'})));}
function estimateAction(title:string,source:NowAction['source']){const lower=title.toLowerCase();const estimatedMinutes=lower.includes('hair wash')?120:lower.includes('workout')?45:lower.includes('email')||lower.includes('reply')?10:lower.includes('water')||lower.includes('supplement')?3:source==='habit'?5:25;const energyCost:NowAction['energyCost']=estimatedMinutes>=45?'high':estimatedMinutes>=20?'medium':'low';return{estimatedMinutes,energyCost};}
function buildSystemHealth(context:PersonalContext):AdaptiveSnapshot['systemHealth']{const habitsDone=context.habits.filter(h=>h.completedToday).length;const habitRatio=context.habits.length?habitsDone/context.habits.length:1;const openReminders=context.appleReminders.filter(item=>!item.completed).length;return[
{domain:'Planning',status:context.overdueTasks.length>=4?'behind':context.overdueTasks.length?'attention':'stable',reason:context.overdueTasks.length?`${context.overdueTasks.length} overdue item${context.overdueTasks.length===1?'':'s'}.`:'No overdue tasks.'},
{domain:'Habits',status:habitRatio<.4?'behind':habitRatio<.75?'attention':'stable',reason:`${habitsDone} of ${context.habits.length} habits complete.`},
{domain:'Calendar',status:context.todaysEvents.length>=7?'attention':'stable',reason:`${context.todaysEvents.length} scheduled event${context.todaysEvents.length===1?'':'s'} today.`},
{domain:'Reminders',status:openReminders>=8?'attention':'stable',reason:`${openReminders} open Apple Reminder${openReminders===1?'':'s'}.`},
{domain:'Focus',status:context.focusScore<45?'behind':context.focusScore<70?'attention':'stable',reason:`Current focus score ${context.focusScore}/100.`}] as AdaptiveSnapshot['systemHealth'];}

export async function getAdaptiveState(userId:string,now=new Date()):Promise<AdaptiveSnapshot>{await ensureAdaptiveDefaults(userId);const context=await buildPersonalContext(userId,now);const[modeRows,rules,inboxRows,maintenance]=await Promise.all([db.select().from(lifeModes).where(and(eq(lifeModes.userId,userId),eq(lifeModes.isActive,true))).limit(1),db.select().from(personalRules).where(and(eq(personalRules.userId,userId),eq(personalRules.enabled,true))).orderBy(desc(personalRules.priority)),db.select({id:glowInboxItems.id}).from(glowInboxItems).where(and(eq(glowInboxItems.userId,userId),eq(glowInboxItems.status,'unprocessed'))),db.select().from(maintenanceForecasts).where(and(eq(maintenanceForecasts.userId,userId),eq(maintenanceForecasts.status,'active'))).orderBy(asc(maintenanceForecasts.dueAt)).limit(8)]);const activeMode=modeRows[0]??null;const availableMinutes=context.nextEvent&&!context.nextEvent.allDay?Math.max(0,Math.floor((context.nextEvent.startAt.getTime()-now.getTime())/60000)-15):null;const modeSlug=activeMode?.slug??'normal';const lowMode=modeSlug==='low-energy'||modeSlug==='sick';const maxMinutes=availableMinutes??90;const actions:NowAction[]=context.recommendations.map((recommendation,index)=>{const source:NowAction['source']=recommendation.href==='/tasks'?'task':recommendation.href==='/habits'?'habit':recommendation.href==='/connections'?'reminder':'routine';const{estimatedMinutes,energyCost}=estimateAction(recommendation.title,source);const base=recommendation.priority==='high'?85:recommendation.priority==='medium'?60:40;const canDoNow=estimatedMinutes<=maxMinutes&&!(lowMode&&energyCost==='high');return{id:recommendation.id,title:recommendation.title,reason:recommendation.reason,href:recommendation.href,source,score:base-index*4+(estimatedMinutes<=maxMinutes?8:-25)+(lowMode&&energyCost==='high'?-30:4),estimatedMinutes,energyCost,canDoNow};});if(context.nextEvent&&availableMinutes!==null&&availableMinutes<=45)actions.push({id:`event-${context.nextEvent.id}`,title:`Prepare for ${context.nextEvent.title}`,reason:'Your next event begins soon. Protect the transition instead of starting something too large.',href:'/calendar',source:'event',score:96,estimatedMinutes:Math.min(20,Math.max(5,availableMinutes)),energyCost:'low',canDoNow:true});const ranked=actions.sort((a,b)=>b.score-a.score);const majorLimit=activeMode?.maxMajorTasks??3;const viable=ranked.filter(item=>item.canDoNow).slice(0,Math.max(1,majorLimit+2));const primary=viable[0]??ranked[0]??null;const alternatives=viable.filter(item=>item.id!==primary?.id).slice(0,4);const protectedItems=modeSlug==='sick'?['Medication','Hydration','Meals','Rest','Critical calendar commitments']:modeSlug==='low-energy'?['Medication','Meals','Hydration','Top priority','Night reset']:modeSlug==='work-day'?['Work','Commute buffer','Meals','One or two personal priorities']:['Top priority','Calendar commitments','Health essentials'];return{context,activeMode:activeMode?{id:activeMode.id,name:activeMode.name,slug:activeMode.slug,maxMajorTasks:activeMode.maxMajorTasks,energyTarget:activeMode.energyTarget}:null,rules:rules.map(rule=>({id:rule.id,title:rule.title,ruleType:rule.ruleType,priority:rule.priority})),now:{availableMinutes,primary,alternatives,protected:protectedItems,hiddenCount:Math.max(0,ranked.length-viable.length)},inboxCount:inboxRows.length,maintenance:maintenance.map(item=>({id:item.id,domain:item.domain,title:item.title,dueAt:item.dueAt,urgency:item.urgency,recommendation:item.recommendation})),systemHealth:buildSystemHealth(context)};}

export async function setActiveLifeMode(userId:string,modeId:string){await db.update(lifeModes).set({isActive:false,updatedAt:new Date()}).where(eq(lifeModes.userId,userId));await db.update(lifeModes).set({isActive:true,updatedAt:new Date()}).where(and(eq(lifeModes.id,modeId),eq(lifeModes.userId,userId)));}
export async function getLifeModes(userId:string){await ensureAdaptiveDefaults(userId);return db.select().from(lifeModes).where(eq(lifeModes.userId,userId)).orderBy(asc(lifeModes.createdAt));}
export async function getInbox(userId:string){return db.select().from(glowInboxItems).where(eq(glowInboxItems.userId,userId)).orderBy(desc(glowInboxItems.createdAt));}
export function classifyInboxText(rawText:string){const value=rawText.trim();const lower=value.toLowerCase();let suggestedType='note';let confidence=.58;if(/\b(call|email|reply|book|schedule|submit|finish|buy|pick up|return|send|do|clean|wash)\b/.test(lower)){suggestedType='task';confidence=.82;}if(/\b(appointment|dentist|doctor|meeting|interview|reservation)\b/.test(lower)){suggestedType='calendar';confidence=.78;}if(/\b(idea|maybe|concept|inspiration|research)\b/.test(lower)){suggestedType='idea';confidence=.72;}if(/\b(goal|save|target|by end of|milestone)\b/.test(lower)){suggestedType='goal';confidence=.7;}if(/\b(buy|order|shop|restock|need more)\b/.test(lower)){suggestedType='shopping';confidence=.75;}return{suggestedType,suggestedTitle:value.slice(0,120),confidence};}
export async function addInboxItem(userId:string,rawText:string,source='manual'){const classified=classifyInboxText(rawText);const[item]=await db.insert(glowInboxItems).values({userId,rawText,source,...classified}).returning();return item;}
export async function markInboxProcessed(userId:string,itemId:string,routedEntityType?:string,routedEntityId?:string){const[item]=await db.update(glowInboxItems).set({status:'processed',routedEntityType,routedEntityId,processedAt:new Date()}).where(and(eq(glowInboxItems.id,itemId),eq(glowInboxItems.userId,userId))).returning();return item??null;}
export async function startFocusSession(userId:string,entityType:string,entityId:string,title:string,plannedMinutes=25){await db.update(focusSessions).set({endedAt:new Date(),completed:false,outcome:'superseded'}).where(and(eq(focusSessions.userId,userId),eq(focusSessions.completed,false)));const[session]=await db.insert(focusSessions).values({userId,entityType,entityId,title,plannedMinutes}).returning();return session;}
export async function finishFocusSession(userId:string,sessionId:string,outcome='completed',notes?:string){const rows=await db.select().from(focusSessions).where(and(eq(focusSessions.id,sessionId),eq(focusSessions.userId,userId))).limit(1);const current=rows[0];if(!current)return null;const endedAt=new Date();const actualMinutes=Math.max(1,Math.round((endedAt.getTime()-current.startedAt.getTime())/60000));const[session]=await db.update(focusSessions).set({endedAt,actualMinutes,outcome,notes:notes??current.notes,completed:outcome==='completed'}).where(and(eq(focusSessions.id,sessionId),eq(focusSessions.userId,userId))).returning();return session??null;}
export async function getActiveFocusSession(userId:string){const rows=await db.select().from(focusSessions).where(and(eq(focusSessions.userId,userId),eq(focusSessions.completed,false))).orderBy(desc(focusSessions.startedAt)).limit(1);return rows[0]??null;}
export async function upsertDayReview(userId:string,dateKey:string,input:{energy?:number;mood?:string;completedSummary?:string;movedSummary?:string;memoryNote?:string;tomorrowTopThree?:string[]}){const rows=await db.select().from(dayReviews).where(and(eq(dayReviews.userId,userId),eq(dayReviews.dateKey,dateKey))).limit(1);if(rows[0]){const[updated]=await db.update(dayReviews).set({...input,updatedAt:new Date()}).where(eq(dayReviews.id,rows[0].id)).returning();return updated;}const[created]=await db.insert(dayReviews).values({userId,dateKey,...input}).returning();return created;}
export async function getTodayReview(userId:string,dateKey:string){const rows=await db.select().from(dayReviews).where(and(eq(dayReviews.userId,userId),eq(dayReviews.dateKey,dateKey))).limit(1);return rows[0]??null;}
