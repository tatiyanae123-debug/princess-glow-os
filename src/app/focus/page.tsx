import { auth } from '@/auth';
import { redirect } from 'next/navigation';
import { AppShell } from '@/components/app-shell';
import { getTasksByUser } from '@/lib/data/tasks';
import { getActiveFocusSession } from '@/lib/intelligence/adaptive-os';
import { finishFocusSessionFormAction, startFocusSessionAction } from '@/app/actions/adaptive-os';
import { Clock3, Focus, Play, CheckCircle2 } from 'lucide-react';

export const dynamic='force-dynamic';

export default async function FocusPage(){const session=await auth();if(!session?.user?.id)redirect('/sign-in');let active=null;try{active=await getActiveFocusSession(session.user.id);}catch{return <AppShell><div className="mx-auto max-w-3xl rounded-[22px] border border-amber-200 bg-amber-50 p-6"><p className="text-sm font-semibold">Focus Sessions need intelligence activation.</p><a href="/settings/intelligence" className="mt-3 inline-block text-xs text-amber-900">Activate intelligence →</a></div></AppShell>;}const tasks=(await getTasksByUser(session.user.id)).filter(t=>t.status!=='done'&&t.status!=='cancelled').slice(0,12);return <AppShell><div className="mx-auto max-w-5xl space-y-5"><header><div className="flex items-center gap-2 text-violet-700"><Focus size={17}/><p className="text-[9px] font-bold uppercase tracking-[.2em]">Focus Sessions</p></div><h1 className="glow-display mt-2 text-[42px] leading-none text-[#392e2a]">One thing at a time.</h1><p className="mt-2 max-w-2xl text-[10px] leading-5 text-[#7e6b64]">Start a focused work block from any open task. Glow records the session so project velocity and daily review can use real execution history.</p></header>{active?<section className="rounded-[24px] border border-violet-200 bg-[linear-gradient(135deg,#f4effb,#fffaf7)] p-6"><p className="text-[8px] font-bold uppercase tracking-[.18em] text-violet-700">Active now</p><h2 className="glow-display mt-2 text-[28px] text-[#433631]">{active.title}</h2><div className="mt-3 flex items-center gap-2 text-[9px] text-[#7b6962]"><Clock3 size={12}/>{active.plannedMinutes??25} min planned · started {active.startedAt.toLocaleTimeString('en-US',{hour:'numeric',minute:'2-digit'})}</div><form action={finishFocusSessionFormAction.bind(null,active.id)} className="mt-5 space-y-3"><textarea name="notes" rows={3} placeholder="What did you accomplish?" className="w-full rounded-xl border border-violet-100 bg-white/70 p-3 text-[10px]"/><div className="flex flex-wrap gap-2"><button name="outcome" value="completed" className="flex items-center gap-2 rounded-xl bg-violet-950 px-4 py-2.5 text-[9px] text-white"><CheckCircle2 size={12}/>Complete Focus</button><button name="outcome" value="stopped" className="rounded-xl border border-stone-200 bg-white px-4 py-2.5 text-[9px] text-stone-600">Stop session</button></div></form></section>:<section className="rounded-[20px] border border-emerald-100 bg-emerald-50/50 p-4 text-[9px] text-emerald-800">No focus session is active. Pick the task that deserves uninterrupted attention.</section>}<section className="grid gap-3 md:grid-cols-2">{tasks.length?tasks.map(task=><article key={task.id} className="rounded-[18px] border border-[#e4d7cf] bg-white/72 p-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-medium text-[#493b35]">{task.title}</p><p className="mt-1 text-[8px] uppercase tracking-[.12em] text-[#9b857d]">{task.priority}{task.dueDate?` · due ${task.dueDate.toLocaleDateString('en-US',{month:'short',day:'numeric'})}`:''}</p></div><form action={startFocusSessionAction.bind(null,'task',task.id,task.title,25)}><button type="submit" disabled={!!active} className="flex items-center gap-1 rounded-lg bg-[#40352f] px-3 py-2 text-[8px] text-white disabled:opacity-30"><Play size={10}/>Focus</button></form></div></article>):<p className="text-[10px] text-stone-500">No open tasks.</p>}</section></div></AppShell>}
