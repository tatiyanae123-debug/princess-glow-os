// Legacy import compatibility. The optimized shell and the canonical shell are
// now one implementation so navigation, context memory, and global controls do
// not drift between routes.
export { AppShell } from '@/components/app-shell';
